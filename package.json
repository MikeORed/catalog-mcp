{
  "name": "catalog-mcp",
  "version": "0.1.0",
  "description": "MCP server for catalog management",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "echo \"Test runner will be added in Phase 5\" && exit 0",
    "test:unit": "echo \"Unit tests will be added in Phase 2\" && exit 0",
    "test:integration": "echo \"Integration tests will be added in Phase 5\" && exit 0"
  },
  "keywords": [
    "mcp",
    "model-context-protocol"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
